<h1>All The Guests</h1>
<!-- index page -->

<ul>
  <% @guests.each do |guest| %>
    <li><%= link_to(guest.name, guest) %></li>
    <li><%= link_to(guest.occupation, guest) %></li>
  <% end %>
</ul>
